<div class="container">
    <div class="row" style="margin-top: 5%">
        {% for video in videosGaleria %}
            <div class="col-md-4 col-xs-12">
                <video id="my-video-{{ loop.index0 }}" class="video-js video-js-edit vjs-big-play-centered" 
{#                       poster="{{ asset('uploads/video/galeria/frame/'~video.id~'.png') }}" #}
                       controls preload="auto">
                    <source src="{{ asset('uploads/video/galeria/'~video.arquivo) }}"
                            type='video/mp4'>
                    <p class="vjs-no-js">
                        Para ver este v&iacute; por favor ative JavaScript, e considere atualizar seu navegador para um que 
                        <a href="http://videojs.com/html5-video-support/" target="_blank">suporte v&iacute;deo HTML5</a>
                    </p>
                    <br />
                </video>
                <p class="text-center">{{ video.titulo }}</p>
            </div>
        {% else %}
            <p>Nenhum v&iacute;deo em destaque no momento.</p>
        {% endfor %}
    </div>
    <div class="row">
        <div class="col-md-12">
            <p><a href="{{ path('fa_site_galeria_videos') }}" id="link-todos-videos">Ver Todos</a></p>
        </div>
    </div>
</div>