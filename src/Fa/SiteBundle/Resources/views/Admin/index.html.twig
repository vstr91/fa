{% extends "FaSiteBundle:Page:central.html.twig" %}  
{% block body %}
    {% block topo %} 

    {% endblock %}
    {% block conteudo %}
        <nav class="navbar navbar-default" id="menu">
            <div class="container" id="menu-container">
                <div class="navbar-header">
                    <button type="button" class="navbar-toggle collapsed" data-toggle="collapse"
                            data-target="#menu-collapse" aria-expanded="false">
                        <span class="sr-only">Abrir / Fechar Menu</span>
                        <span class="icon-bar"></span>
                        <span class="icon-bar"></span>
                        <span class="icon-bar"></span>
                    </button>
                    <span class="navbar-brand"><a href="{{ path('fa_site_admin_homepage') }}">&Aacute;rea Administrativa</a></span>
                </div>
                    <a href="{{ path('fos_user_security_logout') }}" class="navbar-text navbar-right">Sair</a>
            </div>
        </nav>
        <div class="container-fluid" style="min-height: 100%;">
            <div class="row">
                <div class="col-md-12">
                    <h4>Ol&aacute;, {{ usuario.username }}!</h4>
                    <p>Seja bem vindo &agrave; central do administrador!</p>
                </div>
            </div>
            <div class="row text-center v-links-administracao">
                <div class="col-md-12">
                    <div class="btn-group">
                        <a href="{{ path('fa_site_admin_tipos_curso') }}" class="btn btn-primary">Gerenciar Tipos de Curso</a>
                        <a href="{{ path('fa_site_admin_cursos') }}" class="btn btn-primary">Gerenciar Cursos</a>
                        <a href="{{ path('fa_site_admin_noticias') }}" class="btn btn-primary">Gerenciar Not&iacute;cias</a>
                        <a href="{{ path('fa_site_admin_eventos') }}" class="btn btn-primary">Gerenciar Eventos</a>
                        <a href="{{ path('fa_site_admin_banners') }}" class="btn btn-primary">Gerenciar Banners</a>
                    </div>
                    <p>&nbsp;</p>
                </div>
                    <div class="col-md-12">
                        <div class="btn-group">
                            <a href="{{ path('fa_site_admin_destaques_menu') }}" class="btn btn-primary">Gerenciar Destaques Menu</a>
                            <a href="{{ path('fa_site_admin_revistas') }}" class="btn btn-primary">Gerenciar Revistas</a>
                            <a href="{{ path('fa_site_admin_videos') }}" class="btn btn-primary">Gerenciar V&iacute;deo</a>
                            <a href="{{ path('fa_site_admin_videos_galeria') }}" class="btn btn-primary">Gerenciar 
                                V&iacute;deos (Galeria)</a>
    {#                        <a href="{{ path('fa_site_admin_templates') }}" class="btn btn-primary">Gerenciar Templates</a>#}
                            <a href="{{ path('fa_site_admin_parametros_email') }}" class="btn btn-primary">Gerenciar Par&acirc;metros de Email</a>
                        </div>
                    </div>
            </div>
        </div>

        <div class="modal fade" id="form-modal" tabindex="-1" role="dialog" aria-labelledby="modal-label" 
             aria-hidden="true">
            <div class="modal-dialog">
                <div class="modal-content">
                    <div class="modal-header">
                        <button type="button" class="close" data-dismiss="modal" aria-label="Close"><span aria-hidden="true">&times;</span></button>
                        <h4 class="modal-title" id="modal-label">Mensagem</h4>
                    </div>
                    <div class="modal-body" id="conteudo-modal">

                    </div>
                    <div class="modal-footer">
                        <button type="button" class="btn btn-default" data-dismiss="modal">Fechar</button>
                    </div>
                </div>
            </div>
        </div>
    {% endblock %}
    {% block rodape %} {% endblock %}
{% endblock %}