<form id="form-video-galeria" 
      action="{{ path('fa_site_admin_videos_galeria_cadastra', {'id_video_galeria': videoGaleria.id}) }}" 
      method="POST" {{ form_enctype(form) }}>
    {{ form_widget(form) }}
    <p>
        <input type="submit" class="hidden" id="btn-submit" value="Cadastrar" />
    </p>
</form>
<script type="text/javascript">
    {#$('#form-parada').submit(function(){
        $(this).ajaxSubmit(function(data){
            console.log(data);
            $(".caixaForm").dialog('close');
            return false;
        });
        return false;
    });#}
    
    $('#btn-editar').click(function(){
        $('#btn-submit').click();
    });
</script>